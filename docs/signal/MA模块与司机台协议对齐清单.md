# MA 模块 × 司机台协议汇总 —— 对齐清单

> 适用对象：轨道线路与信号控制模块（黄豪，`com.bjtu.railtransit.signal`）
> 对照源：`704司机台轨交多系统平台接口协议汇总.docx`（2026-06，含 信号系统 / 司机台 / 视景 / 车辆 四部分）
> 关联：`MA模块与仿真引擎对接接口说明.md`（接口契约已定稿 ACCEPTED）
> 目的：挑出**你负责的部分（MA 计算 + 线路建模）**里，与这份协议**必须对齐**的字段 / 编码 / 单位，并标清归属，避免越界。

---

## 0. 先说结论（一句话版）

这份协议里**和你强相关**的是「（一）信号系统」整节（3.3.1 输入、3.3.2 输出、3.3.3 加删车）+ 各节复用的**信号机/道岔/区段编码约定**。
你当前代码**已对齐**的是：计轴占用、道岔非定位截断、UP/DOWN 镜像、坡度制动修正、fail-safe 默认收紧、compute 接口形态。
**历史必做项（均已落地）**：① 信号机 aspect ② 故障限速 ③ 定位/完整性 ④ 方向编码 ⑤ 哨兵合同 ⑥ maValidity=1.6。另：**对接闭环 G1–G5/G8/G4 + A4 已完成**（见 handoff §12）。
**单位换算**（cm/s↔km/h、mm↔m）按你既定契约留在边界（周/郭的胶水层）做，MA 内部坚持 km/h、m。

> **落地进度（2026-07-08，TASK-10，仅本模块 scope）**：用户明确"只做我模块里跟司机台冲突的部分，scope 外不管"。
> 已落地 ✅：**D1 方向编解码**（`Direction.fromDriverConsole` + `INVALID` + compute 兜底）、**B1 信号机显示**（新增 `SignalAspect` 枚举 + `Signal.aspect` + `eoaFromSignals` 放行逻辑，消 TODO）。
> 限速单位契约（C2）已在《MA模块与仿真引擎对接接口说明》§11 写明 MA 出 km/h，边界打包归郭，无需改代码。
> **阶段 E / P2（2026-07-11）已落地**：A6 故障限速、A9/A10 定位/完整性丢失、B2 道岔 FAIL 枚举、D3 maValiditySec=1.6s、C1 哨兵合同。
> **对接闭环（同日）**：G5 Cycle 灌入 P2；G1 进路 trainId 绑定；G2 计轴 occupied；G3 TSR REST；G4 events；G8 Registry 权威 MA；**A4 loadFactor→gap**。
> 字段由上游填入，MA 消费。对接总表：`handoff/轨道线路与信号控制-交接文档.md`。
>
> **TASK-11 补充（非司机台协议项，顺带记录）**：为解决多模块"列车状态表示不一致"风险，新增 `TrainStateAdapter`（段相对+m/s→绝对 m+km/h 边界转换入口）并补 `computeOne` 本车状态 fail-safe（防本车 NaN 产出 NaN EoA）。详见 [`../../handoff/数据结构复用排查.md`](../../handoff/数据结构复用排查.md) 与 [`../../backend/backend-changelog-11.md`](../../backend/backend-changelog-11.md)。

---

## 1. 已对齐 / 无需改（放心）

| 项 | 协议依据 | MA 现状 | 说明 |
|----|---------|---------|------|
| 计轴占用约束 | （隐含于拓扑） | `eoaFromAxleOccupancy` 读 `occupied` | 已覆盖，仅需确认占用来源字段名 |
| 道岔非安全态截断 | 3.3.2 道岔 0x01/0x02/0x04 | `eoaFromSwitch`：`state==null \|\| !=NORMAL` → 截断 | fail-safe 已对 |
| UP/DOWN 双向镜像 | 3.3.1 上行0x55/下行0xaa | `Direction.UP/DOWN` 全镜像，17+ 断言验证 | 仅缺"无效 0xff"处理 |
| 坡度制动修正 | 视景/车辆 速度位置 | `requiredGap` 含 `a_eff=a_brake+g·permille/1000` | 已含 |
| fail-safe 默认收紧 | 全文安全基调 | 过期/未知→收紧至当前位、限速0 | 已实现 |
| compute 接口形态 | （你已定稿） | 纯函数 + REST 备选 | 协议未改此约定，保持 |

---

## 2. 必须在你 scope 内对齐（改 MA 代码/契约）

### P0 —— 安全关键，联调前必做

#### B1. 信号机实时显示状态（★最核心）—— ✅ 已落地（TASK-10）
- **协议**：3.3.2 信号系统⇒总控 信号机状态编码：`红灯0x01 / 黄灯0x02 / 红黄灯0x03 / 绿灯0x04 / 黄灭0x05 / 红灭0x06 / 绿灭0x07 / 白灯0x08 / 蓝灯0x0A / 红断0x09 / 绿断0x10 / 黄断0x20 / 白断0x30`。⚠️ **注意文档内还有另外两套编码**（视景 TCMS2VIEW：`0x01红/0x02绿/0x03白`；视景表2解码：`0x00无效/0x01红/0x02绿/0x04白/0x10黄/0x40蓝/0x11红黄`）。同一份文档出现 **3 套信号机编码**，是重大对接风险。
- **MA 现状**：`model/Signal.java` **无 aspect 字段**；`eoaFromSignals` 所有前方信号一律按硬边界截断（你的 `TODO(信号状态接入)`）。
- **对齐动作**：
  1. 在 `Signal` 加 `aspect` 字段，建 `SignalAspect` 枚举（建议：`RED / YELLOW / RED_YELLOW / GREEN / GREEN_YELLOW / WHITE / BLUE / DARK(灭/断)`）。
  2. **选 3.3.2 那套作为 MA 的权威编码**（因为 MA 属"信号系统"，是这些状态的生产方）；另两套由总控/视景侧各自解码，不进 MA 核心。
  3. `eoaFromSignals` 改为：**stop 类（红/红黄/黄/灭/断/蓝，依运营规则）才截断；proceed 类（绿/绿黄…）放行**。灭灯/断必须按 fail-safe 当停车（视景解码里灭灯→0 即如此）。
  4. 提供 `SignalAspect.decode(byte)` 工具，供周锦泽的联锁状态灌入。
- **归属**：黄豪（MA 信号约束维度，纯属你 scope）。

#### A6. 每车故障限速 —— ✅ 已落地（阶段 E）
- **协议**：3.3.1 `故障限速`，单位 **cm/s**。⚠️ **文档自相矛盾**：列车1 写 1 字节(0~0xFF)，列车 n 写 2 字节(0~0xFFFF)——需向周/协议作者确认。
- **落地现状（2026-07-11）**：`faultSpeedLimitKmh` 已入 signal `TrainState`；compute 取 min；**G5** 已由 `SignalCycleService`/StatusReport 灌入。
- **归属**：黄豪（限速维度）；上游填字段。

#### A9 / A10. 列车定位丢失 / 完整性丢失 —— ✅ 已落地（阶段 E）
- **协议**：ATP 应用包 `列车定位丢失`(0初始/1未丢失/2丢失)、`列车完整性状态`(完整/不完整)。
- **落地现状（2026-07-11）**：`positionLost` / `integrityLost` → `POSITION_LOSS`；**G5 已灌入** Cycle。
- **归属**：黄豪；上游填字段。

#### D1. 方向编码统一（你是方向定义权威）—— ✅ 已落地（TASK-10）
- **协议**：方向在 4 个子系统有 **4 套编码**——信号系统 `0x55上行/0xaa下行/0xff无效`；司机台信号屏 `0上行/1下行`；视景 `+1正/-1反`；车辆 `float 方向`。且正文明确"**线路的上行/下行方向由信号系统定义**"（§4 其他，line 278）——即 MA 模块就是权威。
- **MA 现状**：`Direction{UP,DOWN}`，无无效态、无编解码。
- **对齐动作**：在 `Direction` 或独立 `DirectionCodec` 提供 `fromProtocol(byte/int)`：
  - `0x55→UP`、`0xaa→DOWN`、`0xff→INVALID`；
  - `0→UP`、`1→DOWN`（信号屏）；
  - `+1→UP`、`-1→DOWN`（视景）；float 同理。
  - `INVALID` → MA 视为 fail-safe：收紧该车 MA（无法判前车/边界）。
- **归属**：黄豪（定义权威 + 编解码；其他子系统按你的枚举转）。

### P1 —— 应做（影响安全余量/正确性）

#### A4/A5/A7. 方向之外的安全输入（载重 / 降级制动）
- **协议**：`列车载重`(kg)、`可用牵引数量`、`可用制动数量`(0~0xFF)。
- **A4 载重（2026-07-11）✅**：`TrainState.loadFactor`（0~1，NaN=未知→额定）；`aEff = aBrake*(1-loadFactorK*load)+g*‰/1000`，`loadFactorK` 默认 0.2；Cycle 从 runtime 灌入。
- **A5/A7 可用牵引/制动数量**：❌ 仍无字段，YAGNI；按额定全制动。
- **归属**：黄豪（A4 已做）；A5/A7 待字段再开。

#### B2. 道岔状态枚举对齐 —— ✅ 已落地（阶段 E）
- **协议**：`0x01定位 / 0x02反位 / 0x04四开`（3.3.2）。
- **MA 现状**：`SwitchState{NORMAL, REVERSE, FAIL}`，`state!=NORMAL`→截断；`fromProtocol/toProtocol` 对齐 0x01/0x02/0x04。
- **对齐动作**：补 `FAIL(四开)` / `UNKNOWN(null)` 显式枚举，明确 `0x01→NORMAL / 0x02→REVERSE / 0x04→FAIL`，与协议字节一一对应（避免以后误判）。
- **归属**：黄豪（小改）。

#### C1. 输出无效哨兵约定 —— ✅ 合同已写死（阶段 E）
- **协议**：ATP→DMI 速度/距离字段，`0xFFFF`(16bit)/`0xFFFFFFFF`(24/32bit) 表示无效；ATP 报文时效 **1.6s**。
- **MA 现状**：输出 `km/h`、`m`，无"无效哨兵"概念（靠 `event` 表达降级）。
- **对齐动作（合同，非 MA 代码）**：明确——当 `event==DEGRADED/MA_EXPIRED` 时，下游向 DMI/司机台发 `0xFFFF`(速度)/`0xFFFFFFFF`(距离)。把这条写进你的接口契约 §4/§7。
- **归属**：黄豪（定义哨兵合同）/ 郭逸晨（转换下发）。

#### D3. MA 有效期与协议周期对齐 —— ✅ 已落地（阶段 E，默认 1.6s）
- **协议周期**：总控→信号 100ms；信号→总控 250ms；ATP 160ms；司机台 PLC/HMI 100ms；视景 100ms；车辆 UDP 20ms；ATP 时效 **1.6s**。
- **MA 现状**：`maValiditySec = 1.6`（对齐 ATP 时效）。
- **对齐动作**：建议把 `maValiditySec` 收到与这些周期/1.6s 时效同量级（例如 ≤1.6s 或略大于 tick 间隔），避免过期判据过松导致"假安全"。属 `MaConfig` 配置项，你调。
- **归属**：黄豪。

### P2 —— 约定/合同类（不改 MA 核心，边界明确即可）

| 项 | 协议 | 你的契约 | 动作 |
|----|------|---------|------|
| A1 列车ID | 1B 0~0xFF | `trainId:String` | 定 ID 编号规则（String↔byte 映射），与周约定 |
| A2 列车速度 | cm/s | `speedKmh` km/h | 转换在周侧；真实车(列车1)速度脉冲单位需确认 |
| A3 积累走行距离 | cm | `positionM` m | 确认"积累走行距离"=绝对里程×100 且原点=起点道岔(0)？与周对齐坐标系 |
| C2 信号屏限速 | `_fSpeedLimit` WORD，单位待定 | `maxSpeedKmh` km/h | 与郭约定单位（建议 km/h 直喂） |
| D2 驾驶模式 | RM/CM/AM/FAM、BLOC/CBTC/IL | MA 不读 mode | **建议本期 MA 模式无关（始终 CBTC 全功能授权），模式裁剪由 ATO/ATP 负责**；确需 MA 内区分再扩 `TrainState.mode`（边界，归郭） |
| D4 单位混乱 | cm/s、mm/s、m、km/h 未定 混用 | MA 内部 km/h、m（已定 §11） | 坚持 MA 接口单位；所有转换在边界由周/郭做，MA 不背单位 |

---

## 3. 不在你 scope（只标合同，别动手）

| 项 | 协议 | 归属 | 你只需保证 |
|----|------|------|-----------|
| A8 施加紧急制动 | 3.3.1 | 下游 ATP/车辆 | 你的 `event` 在须停时明确，郭据以置位 |
| C4 ATP安全输出（紧急制动输出/零速信号） | 3.3.2 开关量 | 郭(ATP) | `event=DEGRADED/MA_EXPIRED` 即"立即停"信号 |
| C3 距下一站距离 | 司机台信号屏 FLOAT | 总控/ATS | MA 只产 EoA，不产"下一站" |
| C5 信号/道岔状态转发 | 3.3.2 | 周(总控) | MA 读到的态与转发编码用**同一套解码**，避免两边各解 |
| B4 边号/SectionID 坐标 | 视景附件2 | 黄(线路建模)+周 | 保证 segId/里程原点与协议边号表同坐标系（编码格式须一致；9号线 vs BJLine01 拓扑可能不同，先对齐格式） |
| A11 加删车 | 3.3.3 | 周(维护集合) | MA 无状态、List 每 tick 传入即可；你只需容错 0xff 方向/未知车 |

---

## 4. 给周/郭的待确认清单（你 scope 内的依赖项）

1. **信号机编码**：文档内 3 套编码，以哪套为 MA 权威？（建议 3.3.2）
2. **故障限速**：列车1 是 1 字节、列车 n 是 2 字节，以哪个为准？单位确认 cm/s。
3. **积累走行距离 vs 绝对里程**：是否里程×100？原点是否=起点道岔(0)？
4. **真实车(列车1)速度脉冲**：单位/极性，是否需要特殊入口。
5. **信号屏 `_fSpeedLimit` 单位**：km/h 还是其他？
6. **边号/SectionID 映射**：老师数据 BJLine01 的 segId 能否映射到视景边号表（或另给映射表）？

---

## 5. 建议落地顺序

1. **B1 信号 aspect**（P0，核心，先加字段+解码+放行逻辑）
2. ~~**A6 故障限速 + A9/A10**~~ ✅ 阶段 E
3. **D1 方向编解码**（P0，权威定义，含 0xff 容错）
4. ~~**B2 / D3 / C1**~~ ✅ 阶段 E
5. ~~**A4 载重**~~ ✅；**A5/A7** 仍 YAGNI
6. **单位/ID 约定 + 与周/郭待确认项**（合同收口；对接见 handoff）
