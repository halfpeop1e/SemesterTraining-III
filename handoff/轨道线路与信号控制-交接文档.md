# 轨道线路与信号控制 —— 模块交接文档

> 模块：轨道线路与信号控制（MA 计算 + 线路建模）
> 负责人：黄豪
> 后端包：`com.bjtu.railtransit.signal`
> 状态：USER GATE ACCEPTED（2026-07-08）；本地 `mvn test` 40 用例全绿；沙箱 214 断言全绿。
> 本文档目的：让负责其他模块的同学/agent 平滑接入本模块、减少冲突。改本模块前请先读本文档的「红线」与「允许改动的耦合点」。

---

## 1. 模块范围

负责**线路/轨道静态建模**与**移动授权（MA）计算**：

- 线路建模：站点/区间/坡度/限速/道岔/折返/临时限速/计轴区段/进路/保护区段/信号机，支持真实 CBTC 数据加载。
- MA 计算：纯函数 `compute(...)` → 每列车 `{EoA 终点里程, 最大速度, basis, event}`；六维约束（前车占用/折返终点/道岔/信号机/计轴/进路+保护区段），UP/DOWN 双向镜像，坡度制动修正，fail-safe 默认收紧。
- 与司机台协议对齐：方向编码（`Direction.fromDriverConsole`）、信号机显示（`SignalAspect` + 到信号屏映射）。

**边界**：只算 MA + 建模。交付给谁、怎么传、`TrainState` 谁填、列车长度来源——不归本模块，由总控(周锦泽)/车载(郭逸晨)定。本模块只暴露 `MovingAuthorityService.compute(...)`。

---

## 2. 覆盖的文件清单

> 仓库根：`SemesterTraining-III/`；后端：`backend/src/main/java/com/bjtu/railtransit/signal/`

### 2.1 后端 main（`signal` 包 + 关联）
| 子包 | 文件 | 作用 |
|------|------|------|
| `domain/` | `AuthorityBasis` `Direction` `MovingAuthority` `SignalAspect` `SignalEvent` `TrainState` | 枚举/领域对象（出参 MA、入参列车状态、信号显示） |
| `model/` | `LineProfile` `TrackSegment` `Switch` `SwitchState` `Signal` `Turnback` `TurnbackType` `AxleCounterSection` `OverlapSection` `Route` `Gradient` `StaticSpeedRestriction` `TemporarySpeedRestriction` `Station` `Platform` `PhysicalSection` `LogicalSection` `Balise` | 线路静态/可变模型 |
| `service/` | `MovingAuthorityService` `TrackConstraintService` `MaConfig` `LineProfileLoader` | MA 核心 + 约束服务 + 配置 + 数据加载 |
| `util/` | `Units` | 单位换算单一入口 |
| `web/` | `SignalController` `MaRequest` | REST 接口 `POST /api/signal/ma` |

> `common/ApiResponse`、`common/HealthController`、`config/CorsConfig` 为公共文件，非本模块独占，改动前与所有模块协商。
> `RailTransitApplication`（启动类）属全局，非本模块。

### 2.2 测试（`backend/src/test/java/com/bjtu/railtransit/signal/`）
- **verification main（沙箱可独立运行）**：`Task2Verification` `Task3Verification` `Task4Verification` `Task5Verification` `SignalDemo` `DownDirectionVerification` `DriverConsoleAlignmentVerification`
- **JUnit 5 包装**：`MovingAuthorityTest` `TrackConstraintTest` `LineProfileLoaderTest` `SignalDemoTest` `DownDirectionTest` `DriverConsoleAlignmentTest`

### 2.3 文档与变更日志
- 变更日志：`backend/backend-changelog-1.md` ~ `backend-changelog-11.md`
- 详细接口说明：[`../docs/signal/MA模块与仿真引擎对接接口说明.md`](../docs/signal/MA模块与仿真引擎对接接口说明.md)（仓库内，接线主文档）
- 对齐清单：[`../docs/signal/MA模块与司机台协议对齐清单.md`](../docs/signal/MA模块与司机台协议对齐清单.md)（仓库内）
- 本交接文档：`handoff/轨道线路与信号控制-交接文档.md`

> 全流程汇报、QA 测试用例、思路梳理、教学资料整理等为个人本地文档（`.docs/`，不在仓库内）。

---

## 3. 对外提供的接口

### 3.1 MA 计算（核心入口）
```java
package com.bjtu.railtransit.signal.service;
public class MovingAuthorityService {
    Map<String, MovingAuthority> compute(LineProfile line, List<TrainState> trains,
                                         Map<String, Route> routes, double nowSec);
    Map<String, MovingAuthority> compute(LineProfile line, List<TrainState> trains, Map<String, Route> routes);
    Map<String, MovingAuthority> compute(LineProfile line, List<TrainState> trains);
}
```
- **纯函数**，无副作用，不持有可变状态；同输入同输出；每 tick 调用一次。
- 返回 `Map<trainId, MovingAuthority>`；`MovingAuthority` = `{endOfAuthorityM(m), maxSpeedKmh(km/h), basis, event, capSignalId, routeId, timestamp}`。
- `event == DEGRADED || MA_EXPIRED` → 立即停车（`maxSpeedKmh` 此时 = `degradedSpeedKmh`，默认 0）。
- 获取实例：Spring 注入 `@Autowired`；非 Spring 手动 `new MovingAuthorityService(new MaConfig())`。

### 3.2 REST（松耦合/跨进程备选）
`POST /api/signal/ma`，入参 `{lineProfile, trains, routes?, nowSec?}`，返回 `ApiResponse{success,message,data}`，`data` = `Map<trainId, MovingAuthority>` 的 JSON。同进程优先直接调 `compute()`（更低延迟）。

### 3.3 给司机台/其他模块的工具方法
- `Direction.fromDriverConsole(int)`：司机台方向 `0=上行/1=下行/其余=INVALID` → `Direction`。
- `SignalAspect.decodeSignalSystem(int)`：协议 3.3.2 信号机编码字节 → `SignalAspect`（未知→null）。
- `SignalAspect.toDriverConsoleScreen()`：→ 司机台信号屏 红(0x01)/绿(0x02)/白(0x03)。
- `LineProfile.locateMileage(segId, offsetCm)` / `segStartM` / `segEndM`：坐标查询（调试/显示用）。

### 3.4 构造 TrainState 的权威入口（`TrainStateAdapter`，TASK-11 新增）
- `TrainStateAdapter.fromSegmentRuntime(line, trainId, segId, offsetM, speedMps, dir, lengthM, ts)`：**段相对 + m/s → 绝对里程 m + km/h**。其他模块喂 MA 前统一调它，不要各写转换、不要新建第二个 TrainState。
- `TrainStateAdapter.fromAbsolute(trainId, positionM, speedKmh, dir, lengthM, ts)`：已知绝对里程时的便捷入口。
- 非法 segId → `positionM=NaN` → `computeOne` 本车状态校验 fail-safe → `DEGRADED`。

---

## 4. 不可修改的核心代码（红线）

> 这些动了会破坏 fail-safe 语义或方向正确性。**联调时若需要改，必须先找黄豪确认**；其他模块不得擅自改。

1. `MovingAuthorityService.computeOne` 的 fail-safe 兜底顺序与六维候选收集逻辑：
   - `direction==null||INVALID` → degraded；`timestamp` 过期 → `MA_EXPIRED`；他车/前车状态未知 → `DEGRADED`。
   - 取「沿行车方向最近的前方边界」的 `min`(UP) / `max`(DOWN) 选取逻辑。
2. `TrackConstraintService` 六维 `eoaFromXxx` 的**方向感知实现**：
   - UP 取 `min` 且 `> 当前位`；DOWN 取 `max` 且 `< 当前位`；无约束返回 `+INF`（勿退回 `-INF`）。
   - 信号机停车余量：UP `m - signalStopMarginM`；DOWN `m + signalStopMarginM`。
3. `MovingAuthorityService.computeOne` 的 fail-safe 兜底顺序（方向→本车状态→过期→邻车未知→六维候选→min/max 选取）；**本车状态校验 `validState(self)` 不得删除**（含 adapter 非法 segId→NaN 场景）。
3. `TrackConstraintService.eoaFromSignals` 的**放行/截断规则**：`aspect != null && isProceed()`(绿灯) 不截断；`null`/停车/灭/断 截断（fail-safe）。
4. `TrackConstraintService.requiredGap` 坡度制动修正公式：`aEff = aBrakeMps2 + G*(permille/1000)`，`aEff = max(aEff, aFloorMps2)`。
5. 枚举值与映射：
   - `Direction` 的 `UP/DOWN/INVALID` 与 `fromDriverConsole` 映射。
   - `SignalAspect` 的 13 个枚举值、`decodeSignalSystem`、`isProceed`(仅 GREEN)、`toDriverConsoleScreen`(GREEN→绿/WHITE→白/其余→红)。
   - `AuthorityBasis` / `SignalEvent` 全部常量。
6. `util/Units` 所有换算函数（cm↔m、km/h↔m/s、0.1‰→‰）：任何数据换算**必须走 Units**，不得在别处另写魔法数。
7. `LineProfileLoader.enrich()` 的换算步骤（含坡度 0.1‰→‰ 第 7 步）：改了会让 MA 制动修正失真（已踩坑，见 changelog-9）。

---

## 5. 允许联调改动的耦合点

> 为对接其他模块**可以改**，但须遵守下列约束；改动后跑本模块自检确保不回归。

| 耦合点 | 可怎么改 | 约束 |
|--------|----------|------|
| `MaConfig` 参数值 | 调阈值（如 `maValiditySec`、`aBrakeMps2`、`signalStopMarginM`） | 不改字段语义；改后跑 verification 复验数值 |
| `Signal.aspect` 赋值 | 联锁侧把 `SignalAspect` 灌进 `Signal.setAspect(...)` | 用 `decodeSignalSystem` 解码，勿自造编码 |
| `Switch.state` 赋值 | 联锁侧灌 `SwitchState.NORMAL/REVERSE` | null/非 NORMAL 一律 fail-safe 截断，勿改此语义 |
| `TrainState` 新增字段 | 后续消费故障限速/定位丢失/完整性等 | 加字段+getter/setter；在 compute 消费时遵守 fail-safe（异常→收紧）；勿动既有字段语义 |
| `SwitchState` 扩枚举 | 补 `FAIL`(四开)/`UNKNOWN` | `NORMAL`/`REVERSE` 不变；新增值按"非 NORMAL → 截断"处理 |
| `LineProfileLoader` 新增数据源/换算步骤 | 适配新字段 | 换算**必须走 `Units`**；新步骤加到 `enrich()` 末尾 |
| `SignalController` 加端点 | 暴露查询/调试接口 | 不改 `compute(...)` 调用语义 |
| 新增 `model` 字段 | 非破坏性加字段 | 不改既有字段类型/语义 |
| 新增 verification/test | 加自检 | —— |

---

## 6. 依赖其他模块的输入（本模块需要别人喂什么）

| 输入 | 提供方 | 形式 | 备注 |
|------|--------|------|------|
| `TrainState`（每 tick） | 仿真引擎/总控 | `List<TrainState>` | `positionM`(m) / `speedKmh`(km/h) / `lengthM` / `direction` / `timestamp`(s) 必填且非 NaN；`lengthM>0` |
| `Route` Map | 总控(周锦泽) | `Map<trainId, Route>` | key 须与 `trainId` 一致；可空 Map |
| `Signal.aspect` | 联锁(周) | 灌入 `SignalAspect` | 未灌入(null)→fail-safe 硬截断 |
| `Switch.state` | 联锁(周) | `SwitchState` | null→fail-safe 截断 |
| `nowSec` | 引擎时钟 | `double`(s) | 须与 `TrainState.timestamp` 同基准 |
| `LineProfile` | 本模块加载/或总控 | `LineProfileLoader.loadFromJsonFile` | 须 `buildMileageIndex()` |

**坐标基准**：里程为绝对里程 m，原点=起点道岔(0)；`LineProfile` 须先 `buildMileageIndex()` 把 `(segId, offsetCm)` 链成绝对里程。

---

## 7. 单位约定（务必遵守）

| 量 | 单位 | 备注 |
|----|------|------|
| 速度 | km/h | 接口层；内部物理换算 m/s |
| 距离/位置/里程 | m | 真实 CBTC 数据常 cm，`LineProfileLoader` 自动换算 |
| 坡度 | ‰ | 负=下坡；真实数据以 0.1‰ 整数存，loader 已 ÷10 |
| 时间 | s | 仿真时刻、反应/制动时间 |
| 加速度 | m/s² | |

> 真实数据 cm/cm/s ↔ m/km/h 的换算**只在 `Units` + `LineProfileLoader` 边界做一次**，MA 内部统一 m/km/h。其他模块不得在 MA 内部塞 cm。

---

## 8. 验证方式

### 8.1 本地
```bash
cd backend
mvn test        # 预期 40 用例全绿
```

### 8.2 沙箱（maven launcher 环境坏时的备选）
git-bash + `MSYS_NO_PATHCONV=1`，JDK17，手动拼 jar classpath（m2 在 `C:/Users/huanghao/.m2/repository`；Spring 6.2.9、jackson 2.19.2、junit 5.12.2）。编译范围 = 全部 main(除 `RailTransitApplication`)+ signal 全部 test；跑 8 个 verification main：

```
Task2=43  Task3=15  Task4=24  Task5=11  SignalDemo=45
DownDirection=19  DriverConsoleAlignment=41  TrainStateAdapter=16   →  214/214 全绿
```

### 8.3 联调验收判据
- 跟车：后车 EoA ≈ 前车车尾 − 净距 − 制动距离（含坡度修正）。
- 道岔失表/信号红灯/计轴占用：列车在边界前停车。
- 前车状态丢失/方向非法：立即 `DEGRADED`、限速归零。
- 绿灯信号：列车可越过、不被误停。
- UP/DOWN 双向对称。

---

## 9. 联调对接步骤（给引擎/总控/车载）

1. 引擎 tick 循环里，在「总控更新 Route/信号/道岔**之后**、ATO**之前**」调 `compute(line, trains, routes, nowSec)`。
2. 总控把联锁状态灌进 `Signal.setAspect(...)` / `Switch.setState(...)`（用 `SignalAspect.decodeSignalSystem` 解码字节）。
3. 司机台方向字段经 `Direction.fromDriverConsole(...)` 解码后填 `TrainState.direction`。
4. ATO/司机台消费 `MovingAuthority`：`endOfAuthorityM` 作停车点、`maxSpeedKmh` 作限速；`event=DEGRADED/MA_EXPIRED` 立即制动。
5. 司机台信号屏显示用 `SignalAspect.toDriverConsoleScreen()` 取 红/绿/白 字节。

> 接入后若 `switch.state`/`signal.aspect` 不再为 null，MA 自动细化，**无需改本模块代码**。

---

## 10. 已知未做项（按用户边界，非本模块司机台协议直接冲突）

故障限速/载重/可用制动数量/定位丢失/完整性丢失 —— 属车辆/ATP 输入；道岔四开枚举、输出 0xFFFF 哨兵、`maValiditySec` 与协议周期对齐 —— 合同类。详见 [`../docs/signal/MA模块与司机台协议对齐清单.md`](../docs/signal/MA模块与司机台协议对齐清单.md)。后续如需做，按「允许改动的耦合点」扩展 `TrainState` 字段并在 compute 消费（遵守 fail-safe）。

---

## 11. 前端可视化（控制中枢）与 mock 数据说明

> 本模块附带的 Web 控制中枢（`frontend-control-center/`）是 MA 计算与线路建模的**验证/监控工具**，非本模块交付物；核心仍是 `compute(...)`。以下内容供联调同学（尤其总控/联锁侧）了解前端现状，并补齐导致 mock 的缺失接口。

### 11.1 工程位置与启动
- 目录：`frontend-control-center/`（React 18 + Vite 5 + TypeScript + antd + Tailwind + ECharts + Leaflet）
- 四个页面：仪表盘 `Dashboard` / 线路信号 `LineSignal` / 调度控制 `Dispatch` / 能源评估 `EnergyEvaluation`
- 启动：后端 `:8080` 起好后，`frontend-control-center/` 下 `npm run dev`（`:5173`，`vite.config.ts` 已把 `/api` 代理到 `:8080`）

### 11.2 前端数据流架构（联调必读）
- **仿真推进共享**：`src/context/SimulationContext.tsx`（`SimulationProvider`）持有仿真快照 + 运行状态 + 推进循环，挂在 `App.tsx` 页面切换层**之上**。切页面不卸载 → 仿真不冻结（已修复"切走即停"问题）。
- **黄豪 MA 计算链路**：`LineSignal` 把调度仿真列车（`snapshot.trains`）映射后喂给 `POST /api/signal/ma`，返回**真实 MA** ✅。前端不持有任何 MA 算法。
- **模块边界**：启动/停止/推进仿真按钮只在 `Dispatch`；`LineSignal` 只读快照，不反向控制仿真。

### 11.3 各页面数据真实性审计（2026-07-08 后端在线实测）
| 页面 | 模块 | 数据来源 | 实测 | 真实性 |
|------|------|----------|------|--------|
| 仪表盘 | 在线列车/告警/健康度/仿真时间 | `getStatus`→`GET /api/signal/status` | 404 | ❌ mock（`onlineTrains` 写死 4、`simTime=Date.now()`） |
| 仪表盘 | 最近信号事件 | `getEvents`→`GET /api/signal/events` | 404 | ❌ 空（`fallbackEvents` 无数据） |
| 线路信号 | 线路拓扑/站点/道岔 | `getLine`→`GET /api/signal/line` | 200 | ✅ 真实 CBTC |
| 线路信号 | 列车位置/速度 | 共享仿真快照 `GET /simulations/snapshot` | 200 | ✅ 真实（调度仿真 T1~T8） |
| 线路信号 | **MA 授权** | `computeMa`→`POST /api/signal/ma` | 200 | ✅ **真实（本模块 compute）** |
| 线路信号 | 事件日志 | `getEvents`→`GET /api/signal/events` | 404 | ❌ 空 |
| 线路信号 | 道岔单操/进路/重开信号/临时限速 | `operateSwitch` 等 → `/api/signal/*` 控制接口 | 404 | ❌ mock（本地改写 `fallbackLineProfile` + 假事件） |
| 调度控制 | 列车/运行图/到站/能耗 | `/simulations/*` + `/dispatch/*` | 200 | ✅ 全真实（无 fallback） |
| 能源评估 | 能耗/停站/准点/舒适/安全 | `POST /api/evaluation/report` | 200 | ⚠️ 真计算 + **前端写死输入**（站名/站位置/计划到站/效率系数硬编码在 `handleRun`） |

> 易混点：`getLine` 失败时的 `fallbackLineProfile` 来自 `public/line-profile.json`，是**真实 CBTC 数据**而非 mock；当前 `/signal/line=200`，未被使用。`T_BAD`/降级列车是 fail-safe 测试注入，仅「降级测试」开关 ON 时出现，不是隐藏 mock。

### 11.4 ⚠️ 需其他模块补齐的接口清单（mock 根因）
下表所列 mock **非本模块（黄豪）责任**，源于对应接口尚未由负责方实现。请接口 owner 按"应返回数据"实现，前端即可自动切真、撤掉 mock（前端函数当前已是"真接口优先、失败才 fallback"，无需改前端代码）。

| 缺失接口 | 现前端行为（mock） | 应提供方 | 应返回的数据 | 备注 |
|----------|--------------------|----------|--------------|------|
| `POST /api/signal/switch/{switchId}/operate` | 道岔单操走 mock（本地改状态+假事件） | **周锦泽（联锁/总控）** | `ControlResult{success,message}`；成功把 `SwitchState` 灌入 `LineProfile.switches[id].state` | 对应 §6 `Switch.state` 由联锁提供 |
| `POST /api/signal/route/build` / `POST /api/signal/route/{routeId}/cancel` | 进路办理/取消走 mock | **周锦泽（联锁/总控）** | `ControlResult`；成功把 `Route` 写回 `LineProfile.routes` | 进路属联锁域 |
| `POST /api/signal/signal/{signalId}/open` | 重开信号走 mock | **周锦泽（联锁/总控）** | `ControlResult`；成功 `Signal.aspect=GREEN` | 信号显示属联锁域 |
| `POST /api/signal/tsr` / `DELETE /api/signal/tsr/{tsrId}` | 临时限速设置/取消走 mock | **周锦泽（联锁/总控）** | `ControlResult`；成功增删 `LineProfile.tsrs` | TSR 属联锁/调度 |
| `GET /api/signal/status` | 仪表盘整页 mock（在线列车写死 4、仿真时间取系统钟） | **周锦泽（总控/集成）** | `SignalStatus{onlineTrains, alertCount, health, simulationTime}`；`onlineTrains`/`simulationTime` 取自仿真活跃列车与仿真时钟 | 在线列车与仿真时钟归仿真引擎（周），健康度可由各模块汇总 |
| `GET /api/signal/events` | 信号事件流为空（仪表盘"暂无事件"） | **周锦泽（总控/集成）聚合** | `SignalEventItem[]`（MA/道岔/信号/TSR/列车/系统 各类事件） | 事件跨域：MA 事件归黄豪、道岔/信号/TSR 归周、列车事件归仿真；建议由总控层统一聚合暴露 |

> 实现后前端无需改代码：以上接口一上线，`getStatus`/`getEvents`/各 `operateXxx` 立即自动走真。

### 11.5 本模块前端待办（黄豪自补，非他人接口问题）
- **能源评估输入硬编码**：`EnergyEvaluation/handleRun` 把站名、站位置、计划到站、效率系数写死在前端。真实数据已存在：站位置来自 `GET /api/signal/line`、计划到站来自 `GET /api/dispatch/plan`（周，已 200）。属前端对接缺口、非接口缺失——后续把 `handleRun` 入参改为读真实数据即可。若计划到站时刻周侧 dispatch plan 未含，请周在 plan 中补充。
- **信号事件发射（可选）**：若信号事件流判定归本模块，需新增 `GET /api/signal/events` 由 `compute` 过程发射 `MA_DEGRADED`/`MA_EXPIRED` 等事件；否则按 §11.4 由周侧集成层聚合（推荐后者，避免跨域重复）。

### 11.6 联调验证路径（必跑）
1. 后端 `:8080` 起 + 前端 `npm run dev` `:5173`。
2. 调度控制 → 点「启动」→ 切到线路信号：应见 T1~T8 真实列车、MA 实时刷新（仿真不冻结）。
3. 切回调度控制：仿真仍在进行（"停止"按钮在），未被中断。
4. 仪表盘/控制面板：在 §11.4 接口未实现前显示 mock/空，属预期；接口上线后自动变真。
